# Neutralize the stock nginx image default server for appliance Compose.
# The provisioning vhost remains the default_server for normal unmatched hosts.
# This file intentionally defines no server block.
